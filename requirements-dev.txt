# Development and test dependencies
pytest>=7.0.0
ruff>=0.1.0
# Use uv for isolated package management
uv>=0.1.0
# Add other dev dependencies as needed, e.g., pytest-cov
# Ruff is listed explicitly for clarity; other tools may be installed globally in CI.
